import React from "react";
import { Input } from "semantic-ui-react";
import "./Find.css";

const Find = () => {
  return (
    <>
      <Input
        size="large"
        icon="search"
        placeholder="Search..."
        // className={search}
      />
    </>
  );
};

export default Find;
